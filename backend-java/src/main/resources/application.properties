server.port=${PORT:5001}

# Database — mirrors backend/.env
spring.datasource.url=jdbc:postgresql://localhost:5432/japanese_note
spring.datasource.username=${DB_USER:garynlfd}
spring.datasource.password=${DB_PASSWORD:}

# Logging — M6: change to DEBUG to see SQL queries
logging.level.root=INFO
